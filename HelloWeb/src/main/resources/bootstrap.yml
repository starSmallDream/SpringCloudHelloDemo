spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: HELLOCONFIGSERVICE
      fail-fast: true

  application:
    name: AppHelloWeb

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka/
  instance:
    instance-id: HelloWeb